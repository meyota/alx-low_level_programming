#include "main.h"

/**
 * mod - remainder of two integers
 * @num1: the first integer
 * @num2: the second integer
 *
 * Return: the result of modulos
 */

int mod(int num1, int num2)
{
return (num1 % num2);
}
